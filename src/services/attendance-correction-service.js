const endpoint=()=>`${window.ABSEN_SUPABASE_CONFIG.projectUrl}/functions/v1/AttendanceCorrections`;
async function call(action,payload={}){const token=localStorage.getItem('auth_token');const response=await fetch(endpoint(),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,token,...payload})});const body=await response.json().catch(()=>({}));if(!response.ok||body.success===false){const error=new Error(body.message||'Koreksi presensi gagal diproses.');error.code=body.code;error.requestId=body.requestId;throw error;}return body.result;}
export const attendanceCorrectionService=Object.freeze({create:(data)=>call('create',data),listMine:()=>call('listMine'),listQueue:(status)=>call('listQueue',{status}),review:(correctionId,status,notes)=>call('review',{correctionId,status,notes})});
