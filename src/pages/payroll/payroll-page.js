import {payrollService} from '../../services/domain-services.js';
import {renderLoading,renderEmpty,renderError} from '../../components/ui-state.js';
import {renderResponsiveDataList} from '../../components/responsive-data-list.js';
export function createPayrollPage({root}){let tab='history';async function load(){renderLoading(root,'Memuat payroll…');try{const data=await payrollService.history();const rows=Array.isArray(data)?data:(data?.items||data?.data||[]);root.innerHTML=`<section class="app-feature-page"><header><div><h2>Payroll</h2><p>Penerbitan dan riwayat slip dalam modul bertahap.</p></div><div class="app-tabs"><button data-payroll-tab="issue">Penerbitan</button><button data-payroll-tab="history">Riwayat</button></div></header><div data-payroll-content></div></section>`;root.querySelectorAll('[data-payroll-tab]').forEach(button=>button.addEventListener('click',()=>{tab=button.dataset.payrollTab;render(rows);}));render(rows);}catch(error){renderError(root,error,{retry:load});}}function render(rows){const target=root.querySelector('[data-payroll-content]');if(tab==='issue'){target.innerHTML='<div class="app-empty"><strong>Penerbitan Slip</strong><p>Form penerbitan legacy tetap digunakan selama migrasi bertahap.</p><button data-open-legacy-payroll>Buka Penerbitan</button></div>';target.querySelector('[data-open-legacy-payroll]')?.addEventListener('click',()=>window.openPayrollIssuance?.());return;}if(!rows.length){renderEmpty(target,'Belum ada riwayat slip.');return;}renderResponsiveDataList(target,{rows,columns:[{key:'Periode',label:'Periode'},{key:'Status',label:'Status'},{key:'Total_Gaji',label:'Total'},{key:'Created_At',label:'Diterbitkan'}],onSelect:(row)=>window.openPayrollSlipDetail?.(row.ID_Slip||row.id)});}return Object.freeze({load});}