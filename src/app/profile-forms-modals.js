(()=>{if(window.AbsenProfileFormsModals)return;const PROFILE_ROOTS=['#view-profile','#view-profil','[data-profile-page]'];function enhanceProfiles(root=document){PROFILE_ROOTS.flatMap(selector=>[...root.querySelectorAll(selector)]).forEach(page=>{page.classList.add('profile-refresh-active');page.querySelectorAll('.profile-section').forEach((section,index)=>{const header=section.querySelector('.profile-section-header');const body=section.querySelector('.profile-section-body');if(!header||!body)return;section.dataset.collapsible='true';if(!section.dataset.open)section.dataset.open=index===0?'true':'false';if(header.dataset.profileToggleBound)return;header.dataset.profileToggleBound='true';header.setAttribute('role','button');header.setAttribute('tabindex','0');header.setAttribute('aria-expanded',section.dataset.open);const toggle=()=>{const open=section.dataset.open!=='true';section.dataset.open=String(open);header.setAttribute('aria-expanded',String(open))};header.addEventListener('click',toggle);header.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();toggle()}})})})}function enhanceModals(root=document){root.querySelectorAll('.modal-card,.app-modal-card').forEach(modal=>{if(!modal.getAttribute('role'))modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true')});root.querySelectorAll('.modal-body input,.modal-body select,.modal-body textarea').forEach(control=>{if(control.dataset.viewportAssist)return;control.dataset.viewportAssist='true';control.addEventListener('focus',()=>setTimeout(()=>control.scrollIntoView({block:'center',behavior:'smooth'}),120))})}let timer;const schedule=()=>{clearTimeout(timer);timer=setTimeout(()=>{enhanceProfiles(document);enhanceModals(document)},80)};function init(){schedule();new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});window.addEventListener('hashchange',schedule);window.addEventListener('resize',schedule,{passive:true});window.visualViewport?.addEventListener('resize',schedule,{passive:true});window.addEventListener('absen:app-ready',schedule)}window.AbsenProfileFormsModals=Object.freeze({init,refresh:schedule});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();
